#!/bin/sh

# Detect if running in Flatpak or system
if [ -f "/app/share/@app_id@/main.js" ]; then
    exec gjs -m /app/share/@app_id@/main.js "$@"
else
    exec gjs -m /usr/share/@app_id@/main.js "$@"
fi
